class Calculator (add,sub,mul,div,per,mean){
  val add=a+b
  val sub=a-b
  val mul=a*b
  val div=a/b
  val per=a%b
  val mean=(a+b)/2
}

def main(args: Array[String]): Unit = {
  var a=10
  var b=20
  println(add)
}
